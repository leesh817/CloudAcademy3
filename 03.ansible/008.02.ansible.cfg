# controller -> ~/.ansible.cfg
[defaults]
ask_pass = false

inventory = ~/inventory

remote_port = 22
remote_user = student

[privilege escalation]
become = false
become_method = sudo
become_user = root
become_ask_pass = false